# Existing Pages project (the dashboard project slug differs from its pages.dev hostname).
name = "sswintelligence"
compatibility_date = "2024-01-01"
pages_build_output_dir = "./dist"
# No Worker routes, private secrets or service bindings belong in this static project.
