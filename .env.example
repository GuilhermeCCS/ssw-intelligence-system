# Public frontend values only. Do not put secrets in this file.
# The local API is selected automatically while the site runs on localhost.
VITE_MP_PUBLIC_KEY=TEST-sua-chave-publica-do-mercado-pago
VITE_GOOGLE_CLIENT_ID=

# Used by Docker and non-local production builds.
API_URL=http://localhost:8080
